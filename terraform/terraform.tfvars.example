# For local `terraform apply` only — the pipeline doesn't use this file.
# Secrets are NOT here: they live in AWS Parameter Store (SecureString) and the
# DB password + bucket name are generated by Terraform. You can usually leave
# this empty and rely on defaults.

# aws_region   = "us-east-1"
# season_start = "1749081600"   # unix seconds; June 5 2026
